9#include<stdio.h>
int main()
{
    printf("Enter the size of the array \n");
    int n,o=1;
    scanf("%d", &n);
    int ar[n];
    for(int i=0;i<n;i++)
    {
        printf("Enter Value for Position %d\n", i);
        scanf("%d", &ar[i]);
    }
    while(o>0)
    {
        printf("Enter the lower limit for the array\n");
        int l;
        scanf("%d", &l);
         printf("Enter the upper limit for the array\n");
        int u;
        scanf("%d", &u);
        printf("\n[ ");
        for(int i=l-1;i<u;i++)
        {
            printf("%d ", ar[i]);
        }
        printf("]\n");
        printf("Press 1 to do the process again, press 0 to end the process\n");
        int k;
        scanf("%d", &k);
        if(k>0)
        {
            o=1;
        }
        else
        {
            o=0;
        }
    }
}
