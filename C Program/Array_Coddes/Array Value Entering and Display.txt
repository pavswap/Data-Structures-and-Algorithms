#include<stdio.h>
int main()
{
    int numbers[10];
    for(int i=0;i<10;i++)
    {
        printf("Please enter the %d number", i+1);
        scanf("%d\n", &numbers[i]);
    }
    for(int i=0;i<10;i++)
    {
        printf("%d\n", numbers[i]);
    }
}