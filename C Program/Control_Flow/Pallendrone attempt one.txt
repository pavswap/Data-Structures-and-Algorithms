# include <stdio.h>
int main()
{
    printf("Enter the size of the array\n");
    int num,k=0,z=0;
    int num_org=num;
    scanf("%d", &num);
    int arr[num];
    int a=0;
    while(num>0)
    {
     a=num%10;
     num=num/10;
     arr[k]=num;
     k++;
     printf("%d\n", a);  
    }
    int arr2[num_org];
    for(int i=num_org;i>0;i--)
    {
        arr2[i]=arr[i];
    }
    for(int j=0;j<num_org;j++)
    {
        if(arr[j]==arr2[j])
        {
            z++;
        }
        else
        {
            z--;
        }
    }
    if(z>0)
    {
        printf("%d is a pallendrone number", num_org);
    }
    else
    printf("%d is not a pallendrone number", num_org);
    
}