#include<stdio.h>
#include<math.h>
int main()
{
    printf("Enter a Number\n");
    int n,k,j=100,x=0,m=0;
    scanf("%d", &n);
    int l=0;
    l=n;
    while(l>0)
    {
        k=l%10;
        l=l/10;
        m++;
    }
    m=pow(10,m);
    printf("%d", x);
    while(n>0)
    {
        k=n%10;
        n=n/10;
        printf("%d\n", k);
        x=x+k*m;
        m=m/10;
        
    }
    printf("%d", x/10);
}